# Environment Variables - OP Target Sales Hub
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# ===========================================
# DATABASE (Required)
# ===========================================
# Neon PostgreSQL connection string
# Get from: https://console.neon.tech/
DATABASE_URL=postgresql://user:password@host.neon.tech:5432/database?sslmode=require

# For migrations/DDL (uses direct connection, not pooled)
# DATABASE_URL_UNPOOLED=postgresql://user:password@host.neon.tech:5432/database?sslmode=require

# ===========================================
# SECURITY (Required)
# ===========================================
# JWT signing secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-here-min-32-chars

# Encryption secret for sensitive data (generate with: openssl rand -base64 32)
ENCRYPTION_SECRET=your-encryption-secret-here-min-32-chars

# ===========================================
# ADMIN SEED (Required for first run)
# ===========================================
# Secret to protect seed endpoint
SEED_SECRET=your-seed-secret-here

# Initial admin credentials (for seeding only)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-strong-admin-password

# ===========================================
# AI SERVICES (Optional - can set via UI)
# ===========================================
# Google Gemini API Key
# Get from: https://aistudio.google.com/apikey
# GEMINI_API_KEY=AIza...

# OpenAI API Key (alternative to Gemini)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# ===========================================
# APPLICATION
# ===========================================
# Environment: development, production
NODE_ENV=development

# Server port (default: 3000)
# PORT=3000

# ===========================================
# INTEGRATION FEATURE FLAGS (Phase 1+)
# ===========================================
# Enable cross-project authentication bridge
INTEGRATION_AUTH_BRIDGE=false

# Enable survey generation from forge leads
INTEGRATION_SURVEY_FROM_LEAD=false

# Enable WhatsApp sending from OP-Target reports
INTEGRATION_SEND_FROM_REPORT=false

# Enable unified lead view combining both systems
INTEGRATION_UNIFIED_LEAD_VIEW=false

# ===========================================
# INTEGRATION SECRETS (Phase 1+)
# ===========================================
# Shared secret for Auth Bridge token exchange with forge
# Generate with: openssl rand -hex 32
# MUST match forge's integration_shared_secret setting
# NEVER expose to frontend or logs
INTEGRATION_SHARED_SECRET=

# Forge API base URL for server-to-server calls
FORGE_API_BASE_URL=http://localhost:8080
