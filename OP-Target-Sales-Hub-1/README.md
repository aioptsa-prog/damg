
# OP Target Sales Hub 🚀 (v2.5 Pro)

نظام تمكين المبيعات الذكي والتحليلي لشركة **الهدف الأمثل للتسويق**.

## 🏗️ البنية التحتية (Infrastructure)
النظام مصمم ليعمل كمنصة SaaS كاملة.

### ميزات متقدمة:
- **AI Auto-Repair**: محرك ذكاء اصطناعي يعالج الأخطاء تلقائياً في حالة عدم تطابق بنية البيانات.
- **Strict JSON Output**: استخدام Gemini 3 Flash لضمان مخرجات متوافقة برمجياً بنسبة 100%.
- **Encryption-at-Rest**: تشفير مفاتيح الربط للواتساب وجداول جوجل برمجياً.
- **Advanced Leaderboard**: نظام نقاط يعتمد على 5 معايير مختلفة للتقييم.

## 🚀 تعليمات التشغيل والرفع (Deployment)

### 1. الرفع اليدوي (Manual Setup)
1. قم بتثبيت **Node.js 20+** على خادم Ubuntu.
2. ارفع مجلد المشروع.
3. قم بتشغيل `npm install` ثم `npm run build`.
4. استخدم **PM2** لتشغيل التطبيق: `pm2 start npm --name "opt-hub" -- start`.
5. استخدم ملف `deployment/nginx.conf` لضبط Nginx و SSL.

### 2. الرفع عبر Docker
```bash
docker-compose up -d --build
```

## 📚 التوثيق البرمجي (OpenAPI)
يمكنك العثور على مواصفات الـ API كاملة في ملف `openapi.json`. يدعم النظام:
- **Auth**: تسجيل دخول آمن مع Rate Limiting.
- **Leads**: إدارة كاملة لدورة حياة العميل.
- **Integrations**: إرسال مباشر عبر WhatsApp API.

## 🧪 الاختبارات (Testing)
لتشغيل الاختبارات الأساسية (Schema & Logic):
```bash
npm test
```

## 🔐 الأمان والخصوصية
- لا تظهر مفاتيح الـ API في الـ Frontend.
- جميع عمليات التصدير يتم تسجيلها في **Audit Log**.
- النظام يطبق **RBAC** (صلاحيات مبنية على الأدوار).

---
بواسطة وكيل التطوير الشامل Senior Full-Stack لشركة الهدف الأمثل.
