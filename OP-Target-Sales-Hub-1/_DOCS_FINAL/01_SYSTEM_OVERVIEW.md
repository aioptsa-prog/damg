# 01_SYSTEM_OVERVIEW - نظرة عامة على النظام

---

## 🏗️ البنية العامة

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (React)                      │
│                    Port 3000 / Vite                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐  │
│  │ Login   │  │Dashboard │  │LeadList  │  │Settings │  │
│  │ Page    │  │Component │  │Component │  │ Panel   │  │
│  └────┬────┘  └────┬─────┘  └────┬─────┘  └────┬────┘  │
│       │            │             │              │       │
│       └────────────┴─────────────┴──────────────┘       │
│                         │                               │
│                   authService.ts                        │
│                  (httpOnly cookies)                     │
│                         │                               │
└─────────────────────────┼───────────────────────────────┘
                          │ HTTP /api/*
┌─────────────────────────┼───────────────────────────────┐
│                    API Layer                             │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │              _auth.ts (RBAC Middleware)             │ │
│  │    requireAuth() │ requireRole() │ canAccessLead() │ │
│  └────────────────────────────────────────────────────┘ │
│                         │                               │
│  ┌──────┬────────┬──────┴───────┬─────────┬──────────┐ │
│  │auth  │logout  │leads         │users    │settings  │ │
│  │seed  │me      │reports       │analytics│tasks     │ │
│  │reset │change  │activities    │logs     │          │ │
│  └──────┴────────┴──────────────┴─────────┴──────────┘ │
│                         │                               │
│                     _db.ts                              │
│              (PostgreSQL via Neon)                      │
└─────────────────────────┼───────────────────────────────┘
                          │
              ┌───────────┴───────────┐
              │   PostgreSQL (Neon)    │
              │   - users              │
              │   - leads              │
              │   - reports            │
              │   - activities         │
              │   - tasks              │
              │   - settings           │
              │   - audit_logs         │
              └───────────────────────┘
```

---

## 📊 التدفقات الرئيسية

### 1. تسجيل الدخول
```
User → Login.tsx → authService.login()
  → POST /api/auth { email, password }
  → bcrypt.compare(password, hash)
  → Set httpOnly cookie (auth_token)
  → Return user (no password_hash)
  → authService.currentUser = user
```

### 2. إنشاء عميل جديد
```
User → LeadForm.tsx → db.saveLead()
  → POST /api/leads (with cookie)
  → _auth.requireAuth() ← checks JWT
  → _auth.canAccessLead() ← IDOR check
  → INSERT INTO leads
```

### 3. توليد تقرير AI
```
User → ReportView.tsx → aiService.generateReport()
  → db.getAISettings() → GET /api/settings/ai
  → Call Gemini/OpenAI API (backend only)
  → POST /api/reports
  → INSERT INTO reports
```

---

## 📁 هيكل المجلدات

```
OP-Target-Sales-Hub-1/
├── api/                 # Backend endpoints (16 files)
│   ├── _auth.ts         # RBAC middleware
│   ├── _db.ts           # Database connection
│   ├── auth.ts          # Login (bcrypt)
│   ├── seed.ts          # Admin creation
│   └── ...
├── components/          # React components (13 files)
├── services/            # Client services (10 files)
│   ├── authService.ts   # Auth (cookies, no localStorage)
│   └── ...
├── .env.example         # Environment template
└── vite.config.ts       # Build config (no secrets!)
```

---

## 🔒 Boundaries

| الحدود | الوصف |
|--------|-------|
| Frontend ↔ API | httpOnly cookies, CORS |
| API ↔ DB | SSL required (Neon) |
| Secrets | ENV only, never in code |
| AI | Backend-only calls |
