name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Run unit tests
        run: npm run test

  # E2E tests run separately (requires more setup)
  # Uncomment when ready to run E2E in CI
  # e2e:
  #   runs-on: ubuntu-latest
  #   needs: build-and-test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - name: Install Playwright browsers
  #       run: npx playwright install --with-deps chromium
  #     - name: Run E2E tests
  #       run: npm run test:e2e
  #       env:
  #         DATABASE_URL: ${{ secrets.DATABASE_URL }}
  #         JWT_SECRET: ${{ secrets.JWT_SECRET }}
